module.exports = {
  title: "pimatic-probe plugin config options"
  type: "object"
  properties:
    debug:
      description: "Debug mode. Writes debug message to the pimatic log, if set to true."
      type: "boolean"
      default: false
}